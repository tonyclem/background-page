function myFunction(p1, p2) {
  return p1 * p2;
}

console.table(myFunction);

let car = "Fiat";

const car1 = {type:"Fiat", model:"500", color:"white"};

const person = {
  firstName: "John",
  lastName: "Doe",
  age: 50,
  eyeColor: "blue"
};

let car = car1;

car.unshift(person);
